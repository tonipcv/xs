apiVersion: v1
kind: Service
metadata:
  name: {{ include "xase-platform.fullname" . }}
  labels:
    {{- include "xase-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xase-platform.selectorLabels" . | nindent 4 }}
