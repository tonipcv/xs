import { describe, it } from 'vitest';

// Skipping TEE attestation tests because the implementation is fake/removed.
describe.skip('TEE Attestation (removed)', () => {
  it('skipped', () => {});
});
